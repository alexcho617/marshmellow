class Game {
  Game(
      {this.records,
      this.players,
      this.keywords,
      this.curretRound,
      this.code,
      this.host,
      this.playerCount,
      this.playerLimit,
      this.timeLimit});
  dynamic records;
  dynamic players;
  dynamic keywords;
  dynamic curretRound;
  dynamic code;
  dynamic host;
  dynamic playerCount;
  dynamic playerLimit;
  dynamic timeLimit = 60;
}
